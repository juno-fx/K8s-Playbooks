name: Lint
on:
  push:
  pull_request:
jobs:
  lint:
    name: Lint
    runs-on: X64
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run the lint target
        shell: bash
        run: |
          make lint
